import React, { useContext } from "react";
import { ComponentF } from "./ComponentF";
import { compContext, userContext } from "../App";

export const ComponentE = () => {
  // const user = useContext(userContext);
  // const comp = useContext(compContext);
  return (
    <div>
      {/* <ComponentF /> */}
      {/* {user} {comp} */}
      <h1>hiii</h1>
    </div>
  );
};
